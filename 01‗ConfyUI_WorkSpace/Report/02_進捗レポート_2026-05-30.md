# 進捗レポート — Portable Comfy Studio

作成日: 2026-05-30
対象: D:\ai（外付けSSDポータブル / グラボなしCPU機での準備フェーズ）

---

## 1. このセッションの成果（要約）

- **グラボなしでComfyUIが起動できることを実証**（CPUモード, 595ノード, Web UI HTTP 200）。
- 準備フェーズ用ツールを整備（検証・実行・ログ抽出）。
- BATの文字化け事故を解消（純ASCII化）。
- **Git + GitHub** で復元ポイントと共有を確立。
- 他AI向けの**ナレッジ（SSOT）**を作成。
- 「画像の代わりにJSONログ」を実装・実証。

---

## 2. 検証で確定した事実

| 事項 | 結果 |
|------|------|
| グラボなしCPU起動 | ✅ 可能（`Device: cpu`、約40秒でUI応答） |
| 実起動のimport失敗 | **1件のみ** = `ComfyUI_Jags_Audiotools`（`pandas`未導入）。以前の「utils衝突6件」は検証ツールの誤検知だった |
| greenbackワークフロー | 使用ノード9種すべて**コア**＋モデル2個（novaOrangeXL_v140 / granblue-klein9b）。カスタムノード不要 |
| ComfyUI本体 | **gitリポジトリ**（現 v0.11.1, commit b0d97089）→ 更新は**戻せる** |
| PyTorch | `2.5.1+cu121`（requirements.txtのtorch指定は版固定なし） |
| 環境サイズ | venv 5.8GB / models 84GB（=git管理外） |

---

## 3. 成果物一覧

| ファイル | 役割 |
|---------|------|
| `D:\ai\ComfyUI_CPU_RUN.bat` | グラボなしでCPU版ComfyUIを起動（UI動作確認用） |
| `D:\ai\ComfyUI_GPU_NVIDIA.bat` | 本番GPU起動（venv化・ドライブレター非依存） |
| `D:\ai\ComfyUI_CPU_TEST.bat` | 環境＆ワークフロー検証（import/ノード/モデル実在・画像なし） |
| `D:\ai\ComfyUI_WORKFLOW_LOG.bat` | ワークフロー→JSONログ抽出（画像なし・起動なし） |
| `…\50_batch\tools\comfyui_test_validate.py` | 検証スクリプト本体 |
| `…\50_batch\tools\comfyui_workflow_to_log.py` | パラメータ抽出スクリプト本体 |
| `…\90_knowledge\現環境スナップショット.md` | 現状の集約（SSOT） |

GitHub: `SHUNSUKE-Ks/Portable-Comfy-Studio-`（テキスト層のみ公開）

---

## 4. 決定事項

- 実行環境は **venv に一本化**（GPU起動BATも venv 化済み）。
- Gitスコープ = `D:\ai` 直下のテキスト層のみ（巨大バイナリ除外）。
- venvの復元は git ではなく `pip freeze` ＋ zip 退避。
- パッケージ更新・破壊的操作は **人間確認後**に実施。

---

## 5. 現在地と次の一手

- ✅ 「家で準備 → 出先で生成」の準備フェーズ基盤が一通り動作。
- 未了の小タスク: `pip install pandas`（残り1件の警告解消）／ログ出力先を原案の `ComfyUI\test_output\logs\` へ切替（ComfyUI内なので要GO）。
- **検討中: ComfyUI バージョンアップ（v0.11.1 → v0.22.0）** → 方針は別紙
  `00_Plan\設計メモ_ComfyUIバージョンアップ方針.md` 参照。

---

*本レポートは実機検証（2026-05-30, venv, CPU, 生成なし）に基づく。*
